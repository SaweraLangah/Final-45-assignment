"use strict";
// Assignment # 31
let usernames = [];
if (usernames.length === 0) {
    console.log("We need to find some users!");
}
else {
    //Greet user
}
// Removing all username ensures the message "we need to find some user!"is printed.
